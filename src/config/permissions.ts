export enum Modules {
  DASHBOARD = "dashboard",
  INVENTORY = "inventory",
  SALES = "sales",
  CUSTOMERS = "customers",
  PURCHASES = "purchases",
  REPORTS = "reports",
  SETTINGS = "settings",
  CLINIC = "clinic",
  PHARMACY = "pharmacy",
  RESTAURANT = "restaurant",
}

export enum Actions {
  READ = "read",
  WRITE = "write",
  DELETE = "delete",
}
